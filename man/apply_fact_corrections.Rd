% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fact_network_functions.R
\name{apply_fact_corrections}
\alias{apply_fact_corrections}
\title{Apply station-specific corrections to FACT data}
\usage{
apply_fact_corrections(
  fact_data,
  station_col = "Station.Name",
  agency_col = "Agency",
  lat_col = "Latitude",
  lon_col = "Longitude",
  station_agency_reassign = NULL,
  station_agency_file = NULL,
  station_name_corrections = NULL,
  station_name_file = NULL,
  master_receivers = NULL,
  master_receivers_file = NULL,
  agency_specific_corrections = list(NASA = list(pattern = "CS|CC", suffix = "-NASA"),
    FWC = list(pattern = "V2LGMX-", replacement = "")),
  verbose = TRUE,
  progress_bar = TRUE
)
}
\arguments{
\item{fact_data}{A \code{data.table} of FACT detections.}

\item{station_col}{Character. Name of station column. Default: \code{"Station.Name"}.}

\item{agency_col}{Character. Name of agency column. Default: \code{"Agency"}.}

\item{lat_col}{Character. Name of latitude column. Default: \code{"Latitude"}.}

\item{lon_col}{Character. Name of longitude column. Default: \code{"Longitude"}.}

\item{station_agency_reassign}{Optional \code{data.table} with columns
\code{Station.Name} and \code{Agency} for station-specific agency reassignments.
If \code{NULL}, reads from \code{station_agency_file}.}

\item{station_agency_file}{Optional path to CSV file for station-specific
agency reassignments.}

\item{station_name_corrections}{Optional \code{data.table} with columns
\code{old_name} and \code{new_name} (and optionally \code{Agency} for agency-specific
corrections). If \code{NULL}, reads from \code{station_name_file}.}

\item{station_name_file}{Optional path to CSV file with station name corrections.}

\item{master_receivers}{Optional \code{data.table} with columns
\code{Station.Name}, \code{Agency}, \code{Latitude}, \code{Longitude} for coordinate
standardization. If \code{NULL}, reads from \code{master_receivers_file}.}

\item{master_receivers_file}{Optional path to master receiver list CSV.}

\item{agency_specific_corrections}{Optional named list of agency-specific
correction rules (e.g., \code{list(NASA = list(pattern = "CS", suffix = "-NASA"))}).}

\item{verbose}{Logical. Print progress. Default: \code{TRUE}.}
}
\value{
A \code{data.table} with corrected station names, agencies, and coordinates.
}
\description{
Applies station name corrections, coordinate fixes, and agency reassignments
based on reference lookup tables. Corrections are applied in sequence:
\enumerate{
\item Station-specific agency reassignments
\item Station name corrections
\item Coordinate updates from master receiver list
}
}
\details{
\strong{Station-Agency Reassignment}: Useful for receivers that changed hands.
Format: \code{Station.Name}, \code{Agency} (new agency assignment).

\strong{Station Name Corrections}: Can be global or agency-specific.
Format: \code{old_name}, \code{new_name}, optionally \code{Agency} (if correction only
applies to specific agency).

\strong{Master Receiver List}: Authoritative source for station coordinates.
Format: \code{Station.Name}, \code{Agency}, \code{Latitude}, \code{Longitude}.

\strong{Agency-Specific Corrections}: For pattern-based modifications like
adding suffixes. Example:

\if{html}{\out{<div class="sourceCode">}}\preformatted{list(
  NASA = list(pattern = "CS", suffix = "-NASA"),
  FWC = list(pattern = "FPCC", replacement = "FPLR")
)
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
fact_corrected <- apply_fact_corrections(
  fact_data = fact_filtered,
  station_agency_file = "Reference Files/station_agency_reassign.csv",
  station_name_file = "Reference Files/station_name_corrections.csv",
  master_receivers_file = "Reference Files/MASTER_RECEIVERS.csv"
)
}

}
\seealso{
\code{\link[=process_fact_agencies]{process_fact_agencies()}}, \code{\link[=merge_fact_databases]{merge_fact_databases()}}
}

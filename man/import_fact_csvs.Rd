% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fact_network_functions.R
\name{import_fact_csvs}
\alias{import_fact_csvs}
\title{Import and consolidate FACT network CSV files}
\usage{
import_fact_csvs(
  csv_dir,
  pattern = "\\\\.csv$",
  datetime_col = "dateCollectedUTC",
  transmitter_col = "tagName",
  station_col = "station",
  receiver_col = "receiver",
  lat_col = "decimalLatitude",
  lon_col = "decimalLongitude",
  poc_col = "contactPOC",
  pi_col = "contactPI",
  verbose = TRUE,
  progress_bar = TRUE
)
}
\arguments{
\item{csv_dir}{Character. Path to directory containing FACT CSV exports.}

\item{pattern}{Optional character. File pattern to match (default: \code{"\\\\.csv$"}).}

\item{datetime_col}{Character. Name of datetime column in source CSVs.
Default: \code{"datecollected"}.}

\item{transmitter_col}{Character. Name of transmitter column in source CSVs.
Default: \code{"tagname"}.}

\item{station_col}{Character. Name of station column in source CSVs.
Default: \code{"station"}.}

\item{receiver_col}{Character. Name of receiver column in source CSVs.
Default: \code{"receiver"}.}

\item{lat_col}{Character. Name of latitude column. Default: \code{"latitude"}.}

\item{lon_col}{Character. Name of longitude column. Default: \code{"longitude"}.}

\item{poc_col}{Character. Name of point-of-contact column. Default: \code{"contact_poc"}.}

\item{pi_col}{Character. Name of principal investigator column. Default: \code{"contact_pi"}.}

\item{verbose}{Logical. Print progress messages. Default: \code{TRUE}.}
}
\value{
A \code{data.table} with standardized columns:
\code{Date.Time}, \code{Station.Name}, \code{Receiver}, \code{Transmitter},
\code{Latitude}, \code{Longitude}, \code{Agency}
}
\description{
Reads all CSV files from a specified directory containing FACT network
detection data and consolidates them into a single \code{data.table}.
Standardizes column names and removes release records.
}
\examples{
\dontrun{
fact_raw <- import_fact_csvs(
  csv_dir = "Z:/FACT/2025 FACT Data Exports/"
)
}

}
\seealso{
\code{\link[=process_fact_agencies]{process_fact_agencies()}}, \code{\link[=apply_fact_corrections]{apply_fact_corrections()}}, \code{\link[=merge_fact_databases]{merge_fact_databases()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_detections_by_date.R
\name{load_detections_by_date}
\alias{load_detections_by_date}
\title{Load & combine per-date detection bundles}
\usage{
load_detections_by_date(
  dir,
  dates = NULL,
  exclude_dates = NULL,
  after_date = NULL,
  verbose = TRUE
)
}
\arguments{
\item{dir}{Character. Root folder where date subfolders live; e.g.
the output of \code{\link[=sort_VRL_logs]{sort_VRL_logs()}}. Each date subfolder should contain
\verb{detections_<date>.RData}.}

\item{dates}{Character vector of date labels (e.g., \code{"2025-02-28"}),
or \code{NULL} to auto-discover all date folders.}

\item{exclude_dates}{Character vector of date labels to exclude
(e.g., \code{c("2025-02-28", "2025-05-16")}). Default \code{NULL}.}

\item{after_date}{Optional character \code{YYYY-MM-DD}. Keep only folders with
dates >= this threshold. Default \code{NULL} (no lower bound).}

\item{verbose}{Logical; print progress/messages. Default: \code{TRUE}.}
}
\value{
A \code{data.table} with combined detections from all found dates.
Adds a \code{date_label} column indicating the source folder (e.g., \code{"2025-02-28"}).
}
\description{
Loads one or more per-date \code{.RData} bundles created by \code{\link[=sort_VRL_logs]{sort_VRL_logs()}}
(each date subfolder contains \verb{detections_<date>.RData} with an
object named \code{det}) and returns a single combined \code{data.table}.
}
\details{
\itemize{
\item If \code{dates} is \code{NULL}, the function discovers all subfolders in \code{dir}
that look like \code{YYYY-MM-DD} and loads them (optionally filtered by
\code{after_date} and \code{exclude_dates}).
\item If \code{dates} is provided, it loads that set minus any in \code{exclude_dates},
and also applies \code{after_date} if given.
\item For each selected date, it opens \verb{dir/<date>/detections_<date>.RData},
extracts \code{det}, adds a \code{date_label} column (the folder name), and row-binds.
\item Missing bundles are skipped with a message; if none are found, it errors.
}
}
\examples{
\dontrun{
setwd("G:/Charlotte Harbor Common/Sawfish/Acoustic Database Backups")
root_dir <- "2025 Receiver Logs"

# 1) Load only these dates:
det_some <- load_detections_by_date(
  root_dir,
  dates = c("2025-02-28", "2025-05-16")
)

# 2) Load ALL available dates except a few:
det_all_but <- load_detections_by_date(
  root_dir,
  dates = NULL,
  exclude_dates = c("2025-02-28", "2025-09-26")
)

# 3) Load ALL dates on/after 2025-05-01:
det_after <- load_detections_by_date(
  root_dir,
  after_date = "2025-05-01"
)
}

}
\seealso{
\code{\link[=sort_VRL_logs]{sort_VRL_logs()}}, \code{\link[=check_duplicate_detections]{check_duplicate_detections()}}, \code{\link[=identify_coverage_gaps]{identify_coverage_gaps()}}
}

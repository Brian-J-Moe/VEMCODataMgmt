% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fact_network_functions.R
\name{process_fact_agencies}
\alias{process_fact_agencies}
\title{Process FACT network agency assignments}
\usage{
process_fact_agencies(
  fact_data,
  agency_col = "Agency",
  unwanted_agencies = NULL,
  unwanted_agencies_file = NULL,
  agency_lookup = NULL,
  agency_lookup_file = NULL,
  verbose = TRUE
)
}
\arguments{
\item{fact_data}{A \code{data.table} of FACT detections (from \code{\link[=import_fact_csvs]{import_fact_csvs()}}).}

\item{agency_col}{Character. Name of agency column. Default: \code{"Agency"}.}

\item{unwanted_agencies}{Optional character vector of agency/PI names to remove.
If \code{NULL}, reads from \code{unwanted_agencies_file}.}

\item{unwanted_agencies_file}{Optional path to CSV file with column \code{"Agency"}
listing agencies to remove. Ignored if \code{unwanted_agencies} provided.}

\item{agency_lookup}{Optional \code{data.table} or \code{data.frame} with columns
\code{pattern} (regex pattern to match) and \code{standardized} (replacement name).
If \code{NULL}, reads from \code{agency_lookup_file}.}

\item{agency_lookup_file}{Optional path to CSV file with \code{pattern} and
\code{standardized} columns. Ignored if \code{agency_lookup} provided.}

\item{verbose}{Logical. Print progress. Default: \code{TRUE}.}
}
\value{
A \code{data.table} with filtered and standardized agency assignments.
}
\description{
Filters unwanted agencies and standardizes agency/PI names according
to reference lookup tables. Operates in two passes:
\enumerate{
\item Remove detections from specified unwanted agencies
\item Reassign agency names using pattern matching
}
}
\details{
The \code{agency_lookup} table should have:
\itemize{
\item \code{pattern}: String or regex pattern to search for (e.g., "Barbieri", "Reyier|Reiyer")
\item \code{standardized}: Replacement agency code (e.g., "FWC-Barbieri", "NASA-Reyier")
}

Patterns are matched in order, so more specific patterns should come first.
}
\examples{
\dontrun{
# Using reference files
fact_filtered <- process_fact_agencies(
  fact_raw,
  unwanted_agencies_file = "Reference Files/unwanted_agencies.csv",
  agency_lookup_file = "Reference Files/agency_lookup.csv"
)

# Using vectors directly
fact_filtered <- process_fact_agencies(
  fact_raw,
  unwanted_agencies = c("Allen Curry", "Marc Trudel"),
  agency_lookup = data.table(
    pattern = c("Barbieri", "Reyier"),
    standardized = c("FWC-Barbieri", "NASA-Reyier")
  )
)
}

}
\seealso{
\code{\link[=import_fact_csvs]{import_fact_csvs()}}, \code{\link[=apply_fact_corrections]{apply_fact_corrections()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fact_network_functions.R
\name{process_fact_workflow}
\alias{process_fact_workflow}
\title{Complete FACT data processing workflow}
\usage{
process_fact_workflow(
  csv_dir,
  reference_dir,
  existing_rdata = NULL,
  output_rdata = NULL,
  unwanted_agencies_file = "unwanted_agencies.csv",
  agency_lookup_file = "agency_lookup.csv",
  station_agency_file = "station_agency_reassign.csv",
  station_name_file = "station_name_corrections.csv",
  master_receivers_file = "MASTER_RECEIVERS.csv",
  validate = TRUE,
  save_validation = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{csv_dir}{Character. Path to directory containing new FACT CSV exports.}

\item{reference_dir}{Character. Path to directory containing reference files.}

\item{existing_rdata}{Optional path to existing FACT database \code{.RData} file.}

\item{output_rdata}{Optional path for saving updated database. If \code{NULL},
saves to \code{paste0(dirname(existing_rdata), "/UPDATED_FACT_detections.RData")}.}

\item{unwanted_agencies_file}{Filename of unwanted agencies CSV in \code{reference_dir}.
Default: \code{"unwanted_agencies.csv"}.}

\item{agency_lookup_file}{Filename of agency lookup CSV in \code{reference_dir}.
Default: \code{"agency_lookup.csv"}.}

\item{station_agency_file}{Filename of station-agency reassignments CSV.
Default: \code{"station_agency_reassign.csv"}.}

\item{station_name_file}{Filename of station name corrections CSV.
Default: \code{"station_name_corrections.csv"}.}

\item{master_receivers_file}{Filename of master receiver list CSV.
Default: \code{"MASTER_RECEIVERS.csv"}.}

\item{validate}{Logical. Run validation checks after merge? Default: \code{TRUE}.}

\item{save_validation}{Logical. Save validation results to CSV? Default: \code{TRUE}.}

\item{verbose}{Logical. Print progress. Default: \code{TRUE}.}
}
\value{
A list containing:
\describe{
\item{fact_detections}{Final combined and corrected \code{data.table}}
\item{merge_stats}{Statistics from merge operation}
\item{validation}{Validation results (if \code{validate = TRUE})}
}
}
\description{
Executes the complete workflow for importing, correcting, and merging
FACT network detection data. This is a convenience wrapper around the
individual processing functions.
}
\details{
This function chains together:
\enumerate{
\item \code{\link[=import_fact_csvs]{import_fact_csvs()}}
\item \code{\link[=process_fact_agencies]{process_fact_agencies()}}
\item \code{\link[=apply_fact_corrections]{apply_fact_corrections()}}
\item \code{\link[=merge_fact_databases]{merge_fact_databases()}}
\item \code{\link[=validate_fact_database]{validate_fact_database()}} (optional)
}

\strong{Reference Files Required} (in \code{reference_dir}):
\itemize{
\item \code{unwanted_agencies.csv}: Single column \code{"Agency"} with agencies to exclude
\item \code{agency_lookup.csv}: Columns \code{"pattern"} and \code{"standardized"} for name standardization
\item \code{station_agency_reassign.csv}: Columns \code{"Station.Name"} and \code{"Agency"} for reassignments
\item \code{station_name_corrections.csv}: Columns \code{"old_name"}, \code{"new_name"}, optional \code{"Agency"}
\item \code{MASTER_RECEIVERS.csv}: Columns \code{"Station.Name"}, \code{"Agency"}, \code{"Latitude"}, \code{"Longitude"}
}
}
\examples{
\dontrun{
# Basic workflow
result <- process_fact_workflow(
  csv_dir = "Z:/FACT/2025 FACT Data Exports/",
  reference_dir = "Reference Files",
  existing_rdata = "UPDATED_FACT_detections.RData"
)

# Access results
fact_detections <- result$fact_detections
validation <- result$validation
}

}
\seealso{
\code{\link[=import_fact_csvs]{import_fact_csvs()}}, \code{\link[=process_fact_agencies]{process_fact_agencies()}}, \code{\link[=apply_fact_corrections]{apply_fact_corrections()}},
\code{\link[=merge_fact_databases]{merge_fact_databases()}}, \code{\link[=validate_fact_database]{validate_fact_database()}}
}

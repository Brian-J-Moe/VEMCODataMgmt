% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sort_VRL_logs.R
\name{sort_VRL_logs}
\alias{sort_VRL_logs}
\title{Sort existing VRL & CSV files by download date}
\usage{
sort_VRL_logs(
  vrl_dir,
  out_dir,
  target_serials = NULL,
  include_dates = NULL,
  after_date = NULL,
  strict_serial_digits = 6,
  overwrite_date_folders = TRUE,
  date_label_format = "\%Y-\%m-\%d",
  combined_rdata_name = sprintf("detections_all_\%s.RData", format(Sys.Date(),
    "\%Y-\%m-\%d")),
  verbose = TRUE,
  show_timing = TRUE
)
}
\arguments{
\item{vrl_dir}{Directory containing \code{.vrl} and/or \code{.csv} files.}

\item{out_dir}{Destination directory that will receive date-named subfolders and combined RData.}

\item{target_serials}{Optional character vector of serials to include (e.g., \code{c("110730", "480535")}). \code{NULL} = all.}

\item{include_dates}{Optional character vector of \code{YYYY-MM-DD} strings to include. \code{NULL} = all.}

\item{after_date}{Optional \code{YYYY-MM-DD}; keep files with date >= this. \code{NULL} = no lower bound.}

\item{strict_serial_digits}{Minimal digits for serial in filename (default 6; use 5 if needed).}

\item{overwrite_date_folders}{If \code{TRUE}, delete any existing date subfolder in \code{out_dir} before writing.}

\item{date_label_format}{Format for subfolder labels. Default is \verb{\%Y-\%m-\%d} (e.g., 2025-02-28).}

\item{combined_rdata_name}{Filename for the combined bundle in \code{out_dir}.
Default \code{paste0("detections_all_", Sys.Date(), ".RData")}.}

\item{verbose}{Logical; print progress messages and bars (default \code{TRUE}).}

\item{show_timing}{Logical; print per-phase and total elapsed times (default \code{TRUE}).}
}
\value{
A list with components:
\describe{
\item{meta_all}{\code{data.table} of all discovered files with parsed dates/serials and keep/drop flags.}
\item{meta_kept}{Subset of \code{meta_all} after filters (serials/dates) applied.}
\item{out_dir}{Output directory path used.}
\item{dates}{Character vector of date labels (subfolders) created.}
\item{n_detections}{Total number of detection records across all dates.}
\item{failed_files}{Character vector of files that failed to read (if any).}
}
}
\description{
Reads a folder with VEMCO/Innovasea receiver log files (both \code{.vrl} and \code{.csv}).
Files are sorted into date-specific subfolders under \code{out_dir}
based on the \code{YYYYMMDD} date of download in the filename (e.g. log VR2Tx_480535_\emph{\strong{20250228}}_1.vrl
was downloaded on 02/28/2025).
In each subfolder, all CSVs are combined and saved to \verb{detections_<date>.RData}.
Finally, all detections across all dates are combined and saved to
\code{detections_all_YYYY-MM-DD.RData} (by default) in \code{out_dir}.
}
\examples{
\dontrun{
setwd("G:/Charlotte Harbor Common/Sawfish/Acoustic Database Backups/2025 Receiver Logs")

result <- sort_VRL_logs(
  vrl_dir = "ReceiverLogs_2025_29Sept2025",
  out_dir = "Logs_by_Date",
  target_serials = c("110730", "480535"),
  after_date = "2025-01-01",
  strict_serial_digits = 6,
  overwrite_date_folders = FALSE
)
}

}
\seealso{
\code{\link[=load_detections_by_date]{load_detections_by_date()}}, \code{\link[=check_duplicate_detections]{check_duplicate_detections()}}, \code{\link[=identify_coverage_gaps]{identify_coverage_gaps()}}
}
